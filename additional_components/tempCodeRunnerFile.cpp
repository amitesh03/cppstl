int num = 10;            // Example integer value
    bitset<32> numBits(num); // Convert integer to bitset with 32 bits

    cout << "Integer as bitset: " << numBits << endl;

    long longValue = 123456789;     // Example long value
    bitset<64> longBits(longValue); // Convert long to bitset with 64 bits

    cout << "Long as bitset: " << longBits << endl;